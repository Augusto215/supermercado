version: "3.9"

services:
  painel-supermercado:
    container_name: painel-supermercado
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8221:3000"
    environment:
      NODE_ENV: production
      TZ: America/Sao_Paulo
      RHID_CSV_PATH: /app/relatorio_2026311_1914.CSV
